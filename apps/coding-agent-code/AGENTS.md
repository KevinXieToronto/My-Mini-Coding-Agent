# Project rules

- This is a pnpm monorepo. Never use npm or yarn.
- Tests live next to their source as `*.test.ts`. Add to the nearest test file.
- Never edit anything under `dist/` — it is generated by `pnpm build`.
- When you finish a change, remind me to run `pnpm changeset`.
